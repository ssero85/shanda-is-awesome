fhand = open('mbox-short.txt')
counts = {}
for line in fhand:
    if line.startswith('From:'):
       line = line.strip()
       atpos = line.find('@')
       domain = line[atpos+1:]
       counts[domain] = counts.get(domain,0)+1
print(counts)
